<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.codelabs.codelabsapps_piket">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Piket">
        <activity
            android:name=".home.HomeActivity"
            android:theme="@style/Theme.Piket.Home"></activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/Theme.Piket.LoginScreen"
            android:excludeFromRecents="true"
            android:noHistory="true"/>
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.Piket.SplashScreen"
            android:excludeFromRecents="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>